def linear_search(arr,target):
  for i in range(len(arr)):
      if arr[i]==target:
        print(f"element found at index {i}")
        return
  print("element is not found at any index ")
my_list=[1,2,3,9,6]
linear_search(my_list,6)
